import { ClickableElementDirective } from './clickable-element.directive';

describe('ClickableElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickableElementDirective();
    expect(directive).toBeTruthy();
  });
});
